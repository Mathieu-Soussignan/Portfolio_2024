# Server-side only — never exposed to the browser.
# Create a key at https://console.mistral.ai/api-keys/
MISTRAL_API_KEY=

# Optional. Defaults to mistral-small-latest (free tier compatible).
MISTRAL_MODEL=mistral-small-latest
